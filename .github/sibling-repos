# Cross-repo siblings cloned by setup-dev-env. The JS recorder flake
# depends on the current trace-format branches, so PR CI must not fall
# back to older PR-base workspace locks for these two repos.
codetracer-trace-format=main
codetracer-trace-format-nim=main
